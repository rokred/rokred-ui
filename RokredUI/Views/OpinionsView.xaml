<?xml version="1.0" encoding="utf-8"?>

<ui:ReactiveContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:RokredUI.ViewModels;assembly=RokredUI"
    xmlns:controls="clr-namespace:RokredUI.Controls"
    xmlns:ui="clr-namespace:ReactiveUI.XamForms;assembly=ReactiveUI.XamForms"
    xmlns:ffimageloadingsvg="clr-namespace:FFImageLoading.Svg.Forms;assembly=FFImageLoading.Svg.Forms"
    x:TypeArguments="vm:OpinionsViewModel"
    x:Class="RokredUI.Views.OpinionsView">

    <Grid>
        <CollectionView x:Name="Opinions">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout>
                        <Label Text="{Binding MyOpinion}" BackgroundColor="Bisque" Margin="8" />
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <Grid x:Name="Loader">
            <ActivityIndicator HorizontalOptions="Center"
                               IsRunning="True"
                               VerticalOptions="Center"
                               HeightRequest="48"
                               WidthRequest="48"
                               BackgroundColor="White" />
        </Grid>

    </Grid>

</ui:ReactiveContentPage>